/* Replaced at container startup by docker-entrypoint.sh when deploying with Docker.
   Local `yarn start`: leave as-is; use frontend/.env REACT_APP_BACKEND_URL. */
window.__APP_CONFIG__ = window.__APP_CONFIG__ || {};
